version: '3.8'

services:
  postgres:
    image: postgres:16
    container_name: ms-processing-db
    environment:
      POSTGRES_DB: ms_processing_ms
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5436:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: